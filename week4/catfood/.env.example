# PostgreSQL 접속 URL (Supabase 는 6543 트랜잭션 풀러 권장)
DATABASE_URL=postgresql://사용자:비밀번호@호스트:6543/postgres

# 관리자 목록 조회용 토큰 — 설정해야 GET /api/inquiries 가 열린다
ADMIN_TOKEN=

# 로컬 서버 포트 (기본 8790)
# PORT=8790

# SSL 강제 (기본: localhost 면 off, 원격이면 on)
# PGSSL=on

# 같은 IP 의 10분당 문의 건수 (기본 5)
# RATE_LIMIT=5
