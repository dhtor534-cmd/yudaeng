# PostgreSQL 접속 URL — 이 값만 채우면 된다
DATABASE_URL=postgresql://사용자:비밀번호@호스트:5432/DB이름

# 서버 포트 (기본 8789)
# PORT=8789

# SSL 강제 (기본: localhost 면 off, 원격이면 on)
# PGSSL=on

# 커넥션 풀 최대 개수 (기본 5)
# PGPOOL_MAX=5
